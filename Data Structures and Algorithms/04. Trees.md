# 04. Trees
- A tree is an abstract model of a hierarchical structure.
- It composes of nodes and edges
- Num of nodes = Num of edges + 1

## Components of a tree
1. Root node = the topmost, foundational element in a hierarchical tree structure, serving as the starting point for all data traversal, branching, and operations. It is unique because it is the only node in the tree that has no parent and from which all other descendant nodes emerge
2. Leaf node = nodes without any child nodes

<img width="833" height="760" alt="image" src="https://github.com/user-attachments/assets/3a64387c-09ff-4dab-bcd0-91b97b569c21" />

## Types of Trees

### Binary Tree
- Trees which have nodes with no more than 2 children.
- If a node has two children, one child is designated as a left child and the other as a right child.

A binary tree can either be a full binary tree, or a complete binary tree
- Full Binary Tree: every node in the tree except the leaves has exactly two children
- Complete Binary Tree: every level of the tree is completely filled except the last level.

<img width="1664" height="663" alt="image" src="https://github.com/user-attachments/assets/94ea4320-1436-4803-b890-da96bf6ff5bf" />

#### Tree Traversals
There are three main ways to traverse (visit) through each node in a binary tree

#### Pre-order traversal
- Print the data at the root
- Recursively print out all data in the left subtree
- Recursively print out all data in the right subtree
- Root → Left → Right

<img width="2258" height="996" alt="image" src="https://github.com/user-attachments/assets/6cba904e-a2b2-4199-8d76-fef5610f37e0" />

#### In-order traversal
- Recursively print out all data in the left subtree
- Print the data at the root
- Recursively print out all data in the right subtree
- Left → Root → Right

<img width="2258" height="996" alt="image" src="https://github.com/user-attachments/assets/a49726b6-7c8e-489c-9336-fed6185fe383" />

#### Post-order traversal
- Recursively print out all data in the left subtree
- Recursively print out all data in the right subtree
- Print the data at the root
- Left → Right → Root

<img width="2258" height="996" alt="image" src="https://github.com/user-attachments/assets/e958fef1-847c-4cef-a35f-0e388457e1b0" />

### Expression Tree
- An expression tree is a binary tree with the following properties:
  - Each leaf is an operand
  - The root and internal nodes are operators
  - Subtrees are subexpressions, with the root being an operator.
- It is used to perform the mathematical
expressions (with Operands and Operators)

<img width="2258" height="996" alt="image" src="https://github.com/user-attachments/assets/0325b221-a8aa-4ea0-a2f9-770bdcceee83" />

<img width="2258" height="996" alt="image" src="https://github.com/user-attachments/assets/580576b6-e9c3-474f-a56b-1433365a6829" />

<img width="2258" height="996" alt="image" src="https://github.com/user-attachments/assets/9c68bca4-980e-47ac-b1cf-9ff77aa4a143" />

### Binary Search Tree (BST)
A Binary Search Tree (BST) is a node-based binary tree data structure that maintains elements in a strictly sorted order to allow for fast data retrieval, insertion, and deletion

#### Properties of BST
- Left Subtree: Every node in the left subtree has a value less than the parent node's value.
- Right Subtree: Every node in the right subtree has a value greater than the parent node's value.
- Recursive Rule: Both the left and right subtrees must also independently be valid Binary Search Trees.
- insertion, deletion can be performed in BST
- BST has a time complexity of O(log n) if the tree is balanced, but it has a time complexityof O(n) for unbalanced trees

#### Searching for a target value in BST
- Compare the value with the element in the root node.
- If the value is equal , the search is successful
- If value is less , search the left
- If value is greater , search the right
- If the subtree is empty , the search is unsuccessful.

<img width="1918" height="974" alt="image" src="https://github.com/user-attachments/assets/adcfb9ce-436e-44fb-8d77-072241e3d236" />

#### Inserting a node in BST
- Initialize the current node with root node
- Compare the key with the current node.
- Move left if the key is less than or equal to the current node value.
- Move right if the key is greater than current node value.
- Repeat steps 2 and 3 until you reach a leaf node.
- Attach the new key as a left or right child based on the comparison with the leaf node’s value.
  
#### Deleting a node in BST


### Self-Balancing Trees
- Automatically limits its height during insertion & deletion for self-balancing.
- For height-balanced binary trees, such as AVL trees & red–black trees, the height is logarithmic O(log⁡ n)
- Splay trees are assess optimized rather than height balanced.
- Treaps are randomized BST

<img width="2257" height="1121" alt="image" src="https://github.com/user-attachments/assets/e2b2282f-4141-4830-bb16-cb3f2d69eed7" />

### Tree Rotations
a tree rotation is an operation that changes the structure of a binary tree without altering the sequence of its elements. It is the fundamental mechanism used in self-balancing binary search trees (like AVL and Red-Black trees) to decrease tree height and improve operational efficiency

#### Types of Rotation
1. Left Rotation
- Used when a node or its subtree becomes too "right-heavy".
- The right child of the current node is moved up to take its place. The original parent node is shifted down to become the new left child.
- Shifts nodes from the right subtree to the left, decreasing the overall height of the right side

2. Right Rotation
- Used when a node or its subtree becomes too "left-heavy".
- The left child of the current node is moved up to take its place. The original parent node is shifted down to become the new right child.
- Shifts nodes from the left subtree to the right, decreasing the overall height of the left side

#### AVL Trees
- AVL is a self-balancing BST
- In AVL trees, the main property states that The difference between heights of left and right subtrees (balance factor) cannot be more than one for all nodes.”
- Searching, insertion, and deletion are exactly the same as BST.

**Step 1: Identify the height of the nodes**
- Height(node) = 1 + max(height(left), height(right))
- Note: The height of the null node or leaf node can either be -1 or 0 depending on the convention used.

**Step 2: Calculate Balance factor of the node**
- The Balance Factor (BF) for a node (X) is the difference in height between its right and left subtrees or difference in height between its left and right subtrees
- The BF is checked after every insertion & deletion, do necessary rotations if out of balance

BF(X) = height(rightSubtree(X)) − height(leftSubtree(X))

**AVL Tree Example:**
- in this example, the height of null node is 0, which results to the leaf node having a height of 1. The Balance Factor (BF) for a node (X) is the difference in height between its right and left subtrees

<img width="814" height="467" alt="image" src="https://github.com/user-attachments/assets/b39e3447-04ce-4d82-96da-004fb500ad05" />

Balance factor (BF) values
- 0: The node is in balance.
- more than 0: The node is "right heavy".
- less than 0: The node is "left heavy".

If BF < -1, or BF > 1, the tree is not balance. Hence, a tree rotation operation is needed. 

**Out of balanced tree scenarios and rotation solutions**

<img width="2258" height="1254" alt="image" src="https://github.com/user-attachments/assets/9f9bbd4b-f59f-4ae4-9698-d1673e6da25b" />

**Example 1: Left-left scenario**
- The unbalanced node and its left child node are both left-heavy.
- Hence, perform A single right rotation on the unnbalanced node.

<img width="2258" height="1254" alt="image" src="https://github.com/user-attachments/assets/28671ac9-00c8-4fa6-825c-830fbb900790" />

**Example 2: Right-right scenario**
- The unbalanced node and its right child node are both right heavy.
- Hence, perform A single left rotation on the unbalanced node.
  
<img width="2258" height="1254" alt="image" src="https://github.com/user-attachments/assets/eb8ba704-56f5-458f-b614-c57c95dc3a38" />

**Example 3: Left-Right scenario**
- The unbalanced node is left heavy, and its left child node is right heavy.
- First do a left rotation on the left child node, then do a right rotation on the unbalanced node.
  
<img width="2258" height="1254" alt="image" src="https://github.com/user-attachments/assets/7f7bed94-ac4c-416c-9641-0ea6cbfd812f" />

**Example 4: Right-Left scenario**
- The unbalanced node is right heavy, and its right child node is left heavy.
- First do a right rotation on the right child node, then do a left rotation on the unbalanced node.
  
<img width="2258" height="1254" alt="image" src="https://github.com/user-attachments/assets/e4558371-13ce-49a8-96c7-dbe7b10d1d58" />

#### Red-Black Tree
In a red-black tree,

**Properties of Red-Black Trees**
1. Node Color: Each node is either red or black.
2. Root Property: The root of the tree is always black.
3. Red Node Property: Red nodes cannot have red children (Red nodes cannot be adjacent).
4. Black Node Property: Every path from a node to its descendant leaves must have the same number of black nodes.
5. Leaf Property: All leaves (NIL nodes) are black.

**Insertion in Red-Black Trees**
- Note: Newly inserted nodes are always red in color

**Example 1: Inserting new node with its parent node being black**

<img width="813" height="293" alt="image" src="https://github.com/user-attachments/assets/4f35c798-1f2b-4bc7-a3b0-580d0d75c656" />

**Example 2: Inserting new node which happens to be the root**

<img width="813" height="293" alt="image" src="https://github.com/user-attachments/assets/374e3c62-ee55-45db-b273-73fa361911ea" />

**Example 3: Inserting new node with its parent node being red (Violation of property 3: Red Node Property)**
- If the uncle node is red in color, recolor parent, grandparent, and uncle nodes
- After the recoloring of the nodes, the grandparent node is red.
- If the grandparent is the root, recolor the root to black, and the property of the Red Black tree will not be violated.
- If the great grandparent is not black, the processes of analysis and rectification continue upward (recursive).

<img width="813" height="327" alt="image" src="https://github.com/user-attachments/assets/a20fa5bb-b6be-4891-bbb3-9d3e882fa922" />

<img width="813" height="327" alt="image" src="https://github.com/user-attachments/assets/9fc8f03d-ca5a-4da1-8e5c-6aead30a0c43" />

- If The new node is the right child of parent, and the parent is the right child of grandparent.
- Rotate Left at the grandparent level
- Recolor the (original) grandparent and (original) parent.
- If the grandparent is now red, and the great grandparent is also red, the processes of analysis and rectification continue upward.

<img width="1856" height="876" alt="image" src="https://github.com/user-attachments/assets/757ed13c-6484-4179-b925-b38efe1015d0" />

<img width="1856" height="876" alt="image" src="https://github.com/user-attachments/assets/278c32a6-d871-4e2e-a57f-97073fbd358c" />

- If the uncle node is black in color, and The new node is the left child of parent, and the parent is the left child of grandparent.
- Rotate Right at the grandparent level
- Recolor the (original) grandparent and (original) parent.
- If the grandparent is now red, and the great grandparent is also red, the processes of analysis and rectification continue upward.

<img width="1856" height="1051" alt="image" src="https://github.com/user-attachments/assets/b287f05d-225d-44b7-8acf-270aa3bdeb1b" />

<img width="1856" height="1051" alt="image" src="https://github.com/user-attachments/assets/f95e2646-e717-4abc-a5f9-504f84e7a353" />

Deletion in Red-Black Trees
