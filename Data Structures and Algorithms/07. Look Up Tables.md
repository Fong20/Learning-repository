# 07. Lookup Tables
- A data structure used to replace runtime computation with a simpler array indexing
- Stores precomputed results for a function or operation, for fast retrieval based on input values
- Commonly used in embedded systems, graphics, cryptography, and digital signal processing
- Improves performance by trading space for time, especially when input range is small and predictable.
- Can be implemented using arrays, hash tables, or associative arrays depending on the application
- Especially useful in real-time systems where speed is critical and computation must be minimized.


## Storing 
- Use numbers as keys (Array Index)
- Use linked list to store additional data

## Simple hashing
Use hashing which converts words to numbers

<img width="1344" height="547" alt="image" src="https://github.com/user-attachments/assets/9a66c0f8-898c-4960-abc7-5cf2f65e2ff6" />

After hashing, it will produce a large unique array element. However, it is impossible to store such number of elements due to memory constraint.

<img width="2255" height="1273" alt="image" src="https://github.com/user-attachments/assets/181ce855-459f-4c71-a169-f43fbc7001b2" />

## Hash Function
- To fix the issue, we take the returned array elements and modulus the size of the table. This process is knowh as hash function.
- Using the modulo operator (%), we squeeze the resulting huge range of numbers into a range about twice as big as the number of items we want to store.

Eg: 
arraySize = numberWords * 2
arrayIndex = hugeNumber % arraySize

### Collision
- However, it is now prone to collision.
- There are thre ways to solve collision, which are open addressing, and separate chaining.

<img width="2255" height="1273" alt="image" src="https://github.com/user-attachments/assets/dc0ac2a1-d9db-4ff0-83e8-01a6c888da10" />

### Open addressing
- When a collision occurs, the algorithm will search the array in some systematic way for an empty cell and insert the new item there , instead of at the index specified by the hash function .
- E.g. If cats hashes to 5,421, but this location is already occupied by parsnip , then we might try to insert cats in 5,422.
- Types of Open Addressing include Linear Probing, Quadratic Probing. and Double Hashing

#### Linear Probing

<img width="2255" height="1273" alt="image" src="https://github.com/user-attachments/assets/f824c51a-339b-4d12-ac09-ca7e7f895d41" />

#### Quadratic Probing

<img width="2255" height="1273" alt="image" src="https://github.com/user-attachments/assets/5c664674-e1a8-4eed-ab77-346fa8671fc2" />

#### Double Hashing

<img width="2255" height="1273" alt="image" src="https://github.com/user-attachments/assets/c5e86d40-e839-4e03-8079-21f62ad62853" />

### Separate Chaining
Separate chaining is different from open addressing in solving collision where instead of inserting for an empty
cell and insert the new item there
key = words 




