# 04. Access Control List (ACL)
- An ACL is a series of IOS commands that are used to filter packets based on information found in the  packet header.
- Works at Layer 3 and Layer 4

## Functions of ACL

<img width="791" height="330" alt="image" src=" https://github.com/user-attachments/assets/3d7cd19e-7654-45f7-855e-55e2acaeb5db" />

Numbered ACL can block / permit host, and network
Extended ACL can block / permit host, network, and specific ports

## Wildcard Mask
- A wildcard mask is like a subnet mask in that it uses the ANDing process to identify which bits in an IPv4 address to match. However, they differ in the way they match binary 1s and 0s. Unlike a subnet mask, in which binary 1 is equal to a match and binary 0 is not a match, in a wildcard mask, the reverse is true
- ACL uses wildcard mask

### Calculating Wildcard Mask
1. Convert the prefix to binary
2. Subtract 255.255.255.255 with the prefix

<img width="808" height="404" alt="image" src="https://github.com/user-attachments/assets/faf7dc29-3ee9-4e8c-b395-c66229aade5c" />

### Wildcard Mask Keywords
host = specify ACL configuration to be applied to specific host
any = specify ACL configuration to be applied to the whole network 

<img width="808" height="404" alt="image" src="https://github.com/user-attachments/assets/d8be2036-f1bb-4382-a6f1-7ae32b89ec25" />

